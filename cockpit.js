// ============================================
// Cockpit Interaction System (minimal)
// Overlays are now HTML-based, no raycasting needed
// ============================================

let enabled = false;

export function initCockpitInteraction() {
  // Placeholder — interactions handled via HTML overlays
}

export function setCockpitEnabled(val) {
  enabled = val;
}
